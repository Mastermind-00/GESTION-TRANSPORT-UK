package gestiontransport;
import java.util.Scanner;
import java.util.ArrayList;
public class MENU {

	public static void main(String[] args) {
		// recuperation de la des information des passagers pour les afficher
	ArrayList<Passager> listePassagers = new ArrayList<Passager>(); 
	int choix;
	Scanner clavier = new Scanner(System.in);
	do {
		System.out.println("===GESTION DE TRANSPORT===");
		System.out.println("1. Ajouter un trajet");
		System.out.println("2. Reserve une place");
		System.out.println("3. Liste des passagers");
		System.out.println("0. Quitter");
		System.out.println("votre choix : ");
		 choix = clavier.nextInt();
		 switch(choix) {
		 case 1:
			 //ajouter un trajet 
			 ajouteTrajet.ajouterTrajet();

public class Trajet {
	// les variables
	int idTrajet, nombrePlaces;
	String lieuDepart, lieuArrive, dateDepart, dateArrive, duree;
	// cration du constructeur d'initialisation (avec paramettre)
	public Trajet(int idTrajet,int nombrePlaces, String lieuDepart,
			String lieuArrive, String dateDepart, String dateArrive, String duree) {
		this.idTrajet = idTrajet;
		this.nombrePlaces = nombrePlaces;
		this.lieuDepart = lieuDepart;
		this.lieuArrive = lieuArrive;
		this.dateDepart = dateDepart;
		this.dateArrive = dateArrive;
		this.duree = duree;
public class ajouteTrajet {

	public static void ajouterTrajet() {
		// definition variable (type chaine de caractere)
int idTrajet, nombrePlaces;
String lieuDepart, lieuArrive, dateDepart, dateArrive, duree;
// Ajout du trajet par la direction des transports
Scanner clavier = new Scanner(System.in);
System.out.println("Veuillez entre la date de depart");
 dateDepart = clavier.nextLine();
 System.out.println("Veuillez entre la date Arrive");
  dateArrive = clavier.nextLine();
 System.out.println("Veuillez entre le lieu de depart");
  lieuDepart = clavier.nextLine();
 System.out.println("Veuillez entre le lieu d'arrive");
  lieuArrive = clavier.nextLine();
 System.out.println("Veuillez entre la duree");
  duree = clavier.nextLine();
 System.out.println("Veuillez entre l'identifiant du trajet");
  idTrajet = clavier.nextInt();
  System.out.println("Veuillez entre le nombre de place");
   nombrePlaces = clavier.nextInt();
   
 //cration de l'objet trajet
 		Trajet monTrajet = new Trajet(idTrajet, nombrePlaces, lieuDepart, lieuArrive, 
                       dateDepart, dateArrive, duree);
 		System.out.println("Trajet ajoute avec succes !");

	}

}

			 break;
		 case 2 :
			 //reserve une place
			 ReservePlace.reserverPlace(listePassagers);
			public class ReservePlace {
	public static void reserverPlace(ArrayList<Passager> listPassagers) {
		// les variables
	String nom, prenom, numeroTelephone;
	int idTrajet, idPassager = 1, idReservation = 1;
	
	// Saisi des information du passager
	Scanner clavier = new Scanner(System.in);
	System.out.println("Veuillez saisir votre nom ");
	 nom = clavier.nextLine();
	System.out.println("Veuillez saisir votre prenom");
	 prenom = clavier.nextLine();
	System.out.println("veuillez saisir votre numero de telephone");
	 numeroTelephone = clavier.nextLine();
	System.out.println("veuillez saisir votre identifiant de trajet");
	 idTrajet = clavier.nextInt();
	 
	 //creation des objets (passager et Reservation)
	 Passager monPassager = new Passager(idPassager, nom, prenom, numeroTelephone);
	 listPassagers.add(monPassager);
	 Reservation maResevation = new Reservation(idReservation, idTrajet, idPassager);
	 System.out.println("Resevation effectue avec succes !");
	 
    

	}

}

break;
		 case 3:
			 //liste des passagers 
			 listPassagers.afficherPassagers(listePassagers);
			 break;
		 case 0:
			 System.out.println("Au revoir !");
			 break;
		 }
		
	} while(choix !=0);
		
		
	}
	

	}

